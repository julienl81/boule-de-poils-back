liip_imagine:
    driver: gd
    filter_sets:
        cache: ~
        product_image:
            cache: default
            filters:
                #quality: 50
                filters:
                # use and setup the "fixed" filter
                fixed:

                    # set the fixed size to "120x90" pixels
                    width: 800
                    height: 600