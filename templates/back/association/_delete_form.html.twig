<form method="post" action="{{ path('app_association_delete', {'id': association.id}) }}" onsubmit="return confirm('Etes vous sûr de vouloir supprimer ceci?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ association.id) }}">
    <button class="btn btn-danger"> <i class="bi bi-trash"></i> Supprimer</button>
</form>
