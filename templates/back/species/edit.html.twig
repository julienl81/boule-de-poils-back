{% extends 'base.html.twig' %}

{% block title %}Edit Species{% endblock %}

{% block body %}
<div class="container w-50 mx-auto">
    <h1>Editer une espèce    
    <a class="btn btn-return float-end" href="{{ path('app_species_index') }}">Retour à la liste</a>
    </h1>

    {{ include('back/species/_form.html.twig', {'button_label': 'Mettre à jour'}) }}
    {{ include('back/species/_delete_form.html.twig') }}
</div>
{% endblock %}
